/**
 * Module dependencies.
 */

import { Types } from 'mongoose';

/**
 * `UserResolver` endpoints.
 */

const userResolver = {
	Query: {},
};

/**
 * Export userResolver.
 */

export default userResolver;
